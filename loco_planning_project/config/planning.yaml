robot_name: "limo0"

# Time Step (dt)
dt: 0.001  
# Physical radius of the Limo robot (meters)
robot_radius: 0.22  

# Extra buffer distance kept from obstacles (meters)
safety_margin: 0.05 

# Resolution for polygon rounding in Clipper2 (meters)
inflation_epsilon: 0.01

# Maximum linear velocity (m/s)
# Used to generate the timed reference trajectory
v_max: 0.5

min_radius: 1.0 # robot minimum steering radius

step_size: 0.05
t_max: 400

# --- Probabilistic Roadmap (PRM) ---
prm:
  n_samples: 200        # Number of random nodes to scatter
  min_connection_distance: 2.0  # 4 * min_radius (0.4) = 1.6m
  max_connection_distance: 4.0  # Prevents edges that are too long/risky
  resolution: 0.05

# --- Exact Cell Decomposition Planner ---
ecd:
  heuristic_weight: 1.0

# --- Approximate Cell Decomposition Planner ---
acd:
  quadtree_max_depth: 6
  quadtree_min_size: 0.2
  k_victim_neighbors: 4
  min_connection_distance: 2.0